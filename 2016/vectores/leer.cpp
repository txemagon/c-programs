#include <stdio.h>
#include <stdlib.h>

#define MAXSTRING 0x1000
/* Uso:
 *  Vector: 1.5, 3.7, 4.9, 8.3, 4.15 <intro>
 *  => Carga en un array dinámico un vector con 5 componentes (double)
 */

int main(int argc, char *argv[]){
    double entrada;
    double *vector;

    while(scanf(" %*[,] %lf", &entrada )){

        vector = () realloc (++terminos double);
        *(vector + terminos-1) <= entrada
    }

    for (int i=0; i<terminos; i++)
        imprimir cada termino

    return EXIT_SUCCESS;
}
