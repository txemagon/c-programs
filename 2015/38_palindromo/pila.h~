#ifndef __PILA_H__
#define __PILA_H__

#include "definiciones.h"

struct Stack {
   char data[MAX];
   int cima;
};

extern char pop(struct Stack* pila);
extern bool push(char p, struct Stack* pila);

#endif
