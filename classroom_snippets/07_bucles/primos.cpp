#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]){
    bool he_encontrado_un_divisor = false;

    int posible_primo = atoi(argv[1]);

    // bucle cogiendo todos los posibles divisores
    // entre 2 y posible_primo - 1

       // -> Probar a ver si divide (da resto 0)
       // Si lo divide he encontrado un divisor


   // Si !he_encontrado_un_divisor
      // digo que es primo
   // si no
      // digo que no es primo. 

    return EXIT_SUCCESS;
}
