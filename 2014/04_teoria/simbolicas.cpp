#include <stdio.h>
#include <stdlib.h>

#define PI   3.14159284
#define PI_2 PI / 2
#define AREA(x) PI * x * x

#define NAVIDAD(x) ((x) + 200)

int main(int argc, char *argv[]) {

    int r = 8;
    int j = 9;

    double area1 = AREA(r);
    double area2 = AREA(j);
    
    double paga = 2 * NAVIDAD(1000);
    double paga = 2 * x + 200;

    return EXIT_SUCCESS;
}
